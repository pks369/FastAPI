# FastAPI
Build a Python service that fetches data from 3 external APIs concurrently, processes it, stores it in a DB, and exposes results via FastAPI. Long-running ingestion must run as a background job so API responses stay fast.
